import type { JSX } from "react";
import React from "react";
import { Home } from "./features";

const App = (): JSX.Element => {
  return (
    <React.Fragment>
      <Home />
    </React.Fragment>
  );
};

export default App;
